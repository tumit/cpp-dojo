sudo: false

language:
  - cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

script:
  - cd gtest
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make install
  - cd ../..
  - ls gtest/googletest/include/
  - ls gtest/build/googlemock/gtest/
  - g++ -Igtest/googletest/include/ -Lgtest/build/googlemock/gtest/ -lgtest FizzBuzzTest.cpp -o FizzBuzzTest.o
  - ./FizzBuzzTest.o

notifications:
  email: false
